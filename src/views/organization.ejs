<!--Week03 #4 STEP4-->
<%- include('partials/header') %>
<main>
    <h1><%= title %></h1>

    <h2><%= organizationDetails.name %></h2>
    <img src="/images/<%= organizationDetails.logo_filename %>" alt="<%= organizationDetails.name %> logo">

    <p>Description: <%= organizationDetails.description %></p>
    <!--Added on week04 #4 STEP 1-->
    <!-- UPDATED ON WEEK05 TA STEP 09 -->
    <!-- Look for your existing edit link and modify it -->
<% if (user && user.role_name === 'admin') { %>
    <p><a href="/edit-organization/<%= organization.organization_id %>">Edit Organization Details</a></p>
<% } %>

    <h3>Service Projects</h3>
    <ul>
        <% projects.forEach(project => { %>
            <li><a href="/project/<%= project.project_id %>"><%= project.title %></a></li>
        <% }) %>
    </ul>

    <!-- Add this link on your project profile layout template -->


</main>


